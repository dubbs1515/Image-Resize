{
	"name": "image-shrink",
	"version": "1.0.0",
	"description": "Application to optimize image size",
	"main": "main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "electron .",
		"dev": "nodemon --exec electron ."
	},
	"author": "Christopher Dubbs",
	"license": "MIT",
	"devDependencies": {
		"electron": "^10.1.2",
		"nodemon": "^2.0.4"
	}
}
